name: CI/CD Pipeline

on: [push, pull_request]  # Run on code push or pull requests

jobs:
  build:
    runs-on: ubuntu-latest  # Use latest Ubuntu runner

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2  # Pull latest code

      - name: Set up Python
        uses: actions/setup-python@v3
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt  # Install project dependencies

      - name: Run Tests
        run: pytest  # Execute test cases

  deploy:
    needs: build  # Deploy only if build passes
    runs-on: ubuntu-latest

    steps:
      - name: Deploy to Cloud Server
        run: |
          echo "Deploying application..."
          # Add deployment commands for AWS, DigitalOcean, or other cloud providers
